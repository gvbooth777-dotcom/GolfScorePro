//
//  MatchState.swift
//  GolfScorePro
//
//  Match play and skins engine stubs / references.
//

// Match play types and 2v2 Net Better Ball engine are implemented in:
//   MatchTypes.swift        — MatchStatus, TeamHoleOutcome, MatchSummary, TeamMatchRow
//   NetBetterBallEngine.swift — full 2v2 engine
//   TeamTypes.swift         — TeamID, TeamAssignment, TeamScore
//   TeamAggregator.swift    — BestBall, SumAll

// Skins engine and types are implemented in:
//   SkinsEngine.swift  — individual gross skins engine (v1)
//   SkinsTypes.swift   — SkinsHoleOutcome, SkinsSummary, SkinsRow
